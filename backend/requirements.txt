# Phase 1 — core API
# Pinned to versions with published wheels for Python 3.14 (as well as 3.12/3.13).
fastapi==0.141.1
uvicorn[standard]==0.52.3
pydantic==2.13.4
pydantic-settings==2.15.0
python-multipart==0.0.32
PyMuPDF==1.26.0

# Phase 6 — scanned page handling (inpainting + OCR)
opencv-python-headless==4.13.0.92
numpy==2.4.4
pytesseract==0.3.13
Pillow==12.1.1

# ocrmypdf and its heavier system-tool dependencies (Ghostscript, qpdf)
# are not used — OCR is implemented directly via pytesseract + PyMuPDF
# invisible-text insertion (see app/services/ocr_service.py) for a
# lighter, more controllable toolchain. Kept here for reference only:
# ocrmypdf==16.6.0
