# Phase 1 — core API
# Pinned to versions with published wheels for Python 3.14 (as well as 3.12/3.13).
fastapi==0.141.1
uvicorn[standard]==0.52.3
pydantic==2.13.4
pydantic-settings==2.15.0
python-multipart==0.0.32
PyMuPDF==1.26.0

# Phase 2+ (not required yet — kept here so the environment doesn't
# need to be rebuilt when later phases add analysis/processing):
# opencv-python-headless==4.10.0.84
# Pillow==12.1.1
# pytesseract==0.3.13
# ocrmypdf==16.6.0
